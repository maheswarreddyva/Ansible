---
- name: create a file
  hosts: all
  tasks:
    - name: create a file
      ansible.builtin.file:
        path: ~/file.txt
        state: touch
        owner: root
        group: root
        mode: "0777"
