- name: install nginx
  hosts: all
  become: yes
  tasks:
    - name: copy file
      ansible.builtin.copy:
        src: ~/ngix.yaml
        dest: ~/
    - name: ls
      ansible.builtin.shell: "ls -la"
      register: info
    - name: print
      ansible.builtin.debug:
        msg: {{info}} 
